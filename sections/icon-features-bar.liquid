<div class="icon-features-bar">
  <div class="features-wrapper page-width">
    {% for block in section.blocks %}
      <div class="feature-item">
        {% if block.settings.image %}
          <img
            src="{{ block.settings.image | image_url: width: 40 }}"
            alt="{{ block.settings.text | escape }}"
            width="40"
            height="40"
            loading="lazy"
          >
        {% endif %}
        <p>{{ block.settings.text }}</p>
      </div>
    {% endfor %}

    


    {% comment %} {% for block in section.blocks %}
      <div class="feature-item">
        {% if block.settings.image %}
          <img
            src="{{ block.settings.image | image_url: width: 40 }}"
            alt="{{ block.settings.text | escape }}"
            width="40"
            height="40"
            loading="lazy"
          >
        {% endif %}
        <p>{{ block.settings.text }}</p>
      </div>
    {% endfor %} {% endcomment %}
  </div>
</div>

<style>
.icon-features-bar {
  background-color: #000;
  padding: 20px 0;
  color: #fff;
  overflow: hidden; /* hide scroll overflow */
}

.features-wrapper {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 30px;
  text-align: center;
}

.feature-item {
  display: flex;
  align-items: center;
  gap: 12px;
  flex: 1 1 auto;
  min-width: 220px;
  justify-content: center;
  font-size: 14px;
  line-height: 22px;
}

.feature-item img {
  max-width: 40px;
  height: auto;
  filter: brightness(0) invert(1);
}

.feature-item p {
  margin: 0;
}

.feature-divider {
  display: none;
  width: 100%;
  height: 1px;
  border-bottom: 1px dotted #007bff;
  margin: 10px 0;
}

/* Mobile: auto-scrolling loop */
@media screen and (max-width: 768px) {
  .features-wrapper {
    display: inline-flex;
    gap: 40px;
    animation: scrollLoop 20s linear infinite;
  }

  .feature-item {
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    line-height: 20px;
    text-align: center;
  }

  .feature-item img {
    max-width: 20px;
    height: auto;
    filter: brightness(0) invert(1);
  }

  /* Continuous loop, no blank start */
  @keyframes scrollLoop {
    from {
      transform: translateX(0); /* start with first item visible */
    }
    to {
      transform: translateX(-50%); /* move left by half (because we duplicated items) */
    }
  }
}
</style>

{% schema %}
{
  "name": "Icon Features Bar",
  "max_blocks": 5,
  "blocks": [
    {
      "type": "feature",
      "name": "Feature",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Icon Image"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Feature Text",
          "default": "Free Shipping in USA"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Icon Features Bar",
      "blocks": [
        {
          "type": "feature",
          "settings": {
            "text": "Free Shipping in USA"
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "121-Day Warranty"
          }
        },
        {
          "type": "feature",
          "settings": {
            "text": "24/7 Customer Support"
          }
        }
      ]
    }
  ]
}
{% endschema %}
